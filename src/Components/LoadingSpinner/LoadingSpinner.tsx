import React from 'react'

import { Loader, StyledWrapper } from './style'

export default function LoadingSpinner() {
    return (
        <StyledWrapper>
            <Loader />
        </StyledWrapper>
    )
}
